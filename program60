#include <stdio.h>

// Function to calculate area of a circle
float area(float r)
{
    return 3.14 * r * r;
}

int main()
{
    float radius, result;

    printf("Enter the radius of the circle: ");
    scanf("%f", &radius);

    result = area(radius);

    printf("Area of the
